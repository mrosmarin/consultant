packages:
  - "apps/*"
  - "packages/*"

# Native packages allowed to run install scripts (pnpm 10 blocks by default).
# sharp: Next.js image optimization. unrs-resolver: eslint import resolver.
onlyBuiltDependencies:
  - sharp
  - unrs-resolver
