# Active Context

_Last updated: 2026-06-02_

> **PROD AT `e097fba` (2026-06-01).** Recent prod releases: M12 reporting (#79), **RBAC foundation DEV-69 (#81)**, **client portal DEV-140 (#83)**, **team-member role DEV-141 (#85)** — i.e. **RBAC (DEV-69) is fully shipped to prod** (foundation + client portal + team role). `develop` and `main` in sync. Prod Neon at migration **`0028`** (NOT 0005 — migrations had been landing on prod via a mis-pointed dev `.env.local`, now fixed). **Local dev uses a dedicated `dev` Neon branch** (`ep-flat-bonus`), isolated from production; prod was cleaned of all accumulated test data 2026-05-31 (only `mrosmarin@gmail.com` remains). **DEV-79 security audit done** (findings → DEV-79 comment; remediation = DEV-142 RBAC action guards + DEV-143 headers/CSP/upload). **Resume at: DEV-142 + DEV-143 (security remediation), then M11 automation or M6 launch (DEV-82 go-live).**

## Current focus

**Prod fully live (public site + portal + invoicing core); next is the M7–M13 invoicing-platform build-out.**
- **Production:** https://endlessworlds.xyz — public site + secure portal (auth + allowlist + **RBAC roles**, dashboard, **companies/clients**, **timesheets**, **invoicing**, **reports**, admin **access management**, read-only **client portal** `/client`), SEO, GA4 (`G-G830M5YF0W`, consent-gated). Prod Neon at migration **`0027`**.
- **Staging/QA:** https://staging.endlessworlds.xyz — dedicated Neon project, fully isolated (data **and** auth), resettable via `make db-reset-staging`. `noindex`.
- **Roadmap:** `docs/roadmap.md` + Linear M7–M13 — client/project depth → invoice engine v2 (line items, tax, currency, discounts, fixed-fee/milestone) → quotes/docs/delivery → payments → automation → reporting → admin/compliance. Keystone: **DEV-115 invoice line-items**.
- **Remaining M3:** copy polish (DEV-63/64), insights/blog (DEV-59/66).

## Environments

- **Production** — `main` → `endlessworlds.xyz` (+ `endlessworlds-web.vercel.app`). Neon project `dry-darkness-00977469`, **`production` branch** (`ep-orange-glade`) + its Neon Auth. Indexable. GA4 active. At migration `0027`.
- **Local dev** — `apps/web/.env.local` → the prod project's **dedicated `dev` branch** (`br-cool-shadow` / `ep-flat-bonus`), created off production 2026-05-31. **This used to point at the `production` branch itself**, so local round-trips + `db:migrate` wrote to prod and migrations landed there outside the release flow (prod silently advanced 0005→0027). Now isolated. `make worktree-new` copies this `.env.local`, so worktrees inherit the dev branch. **Always check the endpoint host before destructive/migration ops:** `ep-flat-bonus`=dev, `ep-orange-glade`=PROD, `ep-round-wave`=staging.
- **Staging/QA** — `develop` → `staging.endlessworlds.xyz`. **Dedicated Neon project `EndlessWorlds.Staging` = `winter-dew-93819743`** (own DB **and** own Neon Auth) → logins + data fully isolated from prod. Wired via `develop`-branch-scoped Vercel env (`DATABASE_URL`/`_UNPOOLED`/`NEON_AUTH_BASE_URL`/`NEON_AUTH_COOKIE_SECRET`). `noindex`; Vercel SSO/deploy-protection OFF (so all previews are public). Reset: `make db-reset-staging`.
- **PR previews** (non-`develop`) — prod project's `preview` Neon branch (shares prod auth). Only `develop`/staging gets the isolated project.
- **DNS:** registrar **Hover**, Hover nameservers. Vercel hosts the records.
- **`develop` and `main` are in sync** (2026-06-02, after the DEV-79 audit-tracking release) — through RBAC + the DEV-79 security audit. Release flow: branch `feature/dev-XXX-*` off `develop` → PR to `develop` → when ready, PR `develop`→`main`. **Apply migrations to staging AND prod on release** — run `drizzle-kit migrate` FROM THE WORKTREE (not root, which lacks the new migration) against the target branch via a temp config (see [[feedback_migration_filename_no_guess]]); now that dev is a separate branch, prod no longer auto-migrates via dev.

## Recent changes (newest first; deduped at the 2026-06-02 checkpoint)

- **DEV-151 (issuer contact name — 2026-06-16):** Added a **contact name** (a person) to the business-settings issuer, separate from the legal/company name, rendered on the invoice "from" block (PDF + public web view, under the legal name, when set). New `business_settings.contact_name` column (migration **`0030`** — additive `ADD COLUMN`, nullable; passes the DEV-150 guard, no data plan). Field added to Settings → Business identity + save action; `IssuerInfo.contactName` threaded through. Gates green (TYPES=0 LINT=0 BUILD=0); `0030` applied to dev. **First feature through the full automated flow:** worktree → PR → develop (pipeline auto-migrates staging) → main (pipeline auto-migrates prod). (PR pending.)

- **DEV-150 (migration data-safety policy + CI guard — 2026-06-16):** Prod now holds **real data** (owner entered payee/banking details via /account/settings) → policy: **no prod data loss**. Codified in CONTRIBUTING ("Migration data safety"): additive/expand-contract by default; destructive/breaking changes (drop/rename/type-change/NOT NULL/truncate/merge-split) require a **written data-migration plan** (inventory → **Neon prod snapshot** → expand → backfill → verify → contract in a later release → rollback). Added a **guard step to `migrate.yml`** (push events only; `fetch-depth: 0`): scans migrations changed in the push for destructive SQL and **fails the run** unless the file opts in with `-- destructive-approved: <reason>`. Guard regex unit-tested (additive passes, all destructive forms blocked, marker overrides, real 0029 clean). Built **in a worktree** (correcting the session's root-checkout lapse — see [[feedback_worktree_flow]]). See memory [[feedback_no_prod_data_loss]]. (PR pending → develop → main.)

- **DEV-149 (migrations in CI pipeline — 2026-06-16):** Migrations were manual per-env (`make db-migrate` only hits dev via `.env.local`; staging/prod hand-applied). Added **`.github/workflows/migrate.yml`**: on push to **`develop` → `drizzle-kit migrate` against staging**, push to **`main` → against prod** (branch picks the secret; `+ workflow_dispatch` for manual runs). Uses the existing `drizzle.config.ts` (reads `DATABASE_URL_UNPOOLED` from job env; dotenv `.env.local` load is a no-op in CI). Gated on `RUN_CI`; `concurrency cancel-in-progress: false` so a migration is never interrupted. Documented in CONTRIBUTING ("Migrations in CI"). **Owner must set repo secrets `STAGING_DATABASE_URL_UNPOOLED` + `PROD_DATABASE_URL_UNPOOLED` (direct/unpooled) and flip `RUN_CI=true`** (Actions quota was due to reset ~June 2026; still `false` as of build). **Both secrets SET** (prod from `NEON_POSTGRES_DB`; staging fetched via `NEON_API_KEY`); shipped (PR #100→develop, #101→main). **`RUN_CI=true` flipped + both paths verified green** (workflow_dispatch: develop→staging success, main→prod success — no-op since 0029 already applied). **DONE — migrations now auto-apply on merge.** **Discipline going forward: migrations must be expand/contract (additive)** — Vercel deploys on push independently of Actions, so the migrate-vs-deploy race is only safe if new code tolerates the old schema (see DEV-148 fail-safe).

- **DEV-148 (invoice issuer / payee + remit details — 2026-06-16):** Invoices hardcoded "EndlessWorlds, LLC / Levittown, NY" + "contact us for details" (clients couldn't actually pay). Added a DB-backed issuer/remit profile + admin **`/account/settings`** page so the owner enters real details (kept out of git + chat). New owner-scoped **`business_settings`** table (migration **`0029`**, RLS + `business_settings_all` policy + soft-delete; singleton-per-owner upsert): legal name, address, email/phone, tax id, and **all 4 payment methods** (bank/ACH, check-by-mail, online Zelle/Venmo/PayPal/pay-link, free-text note). `src/lib/business-settings.ts` (`getBusinessSettings` + `issuerInfo`/`paymentLines` helpers; **fail-safe — returns null/fallback issuer on any DB error so invoice rendering never breaks if the migration lags the deploy**). Wired the issuer block + dynamic payment instructions into the **invoice PDF** (`invoice-pdf.tsx`, type-only IssuerInfo import) + **public web view** (`/invoice/[token]`) — both read live by the invoice's owner `userId`; fallback to the old hardcoded issuer when unset. Settings in portal nav (admin-only). Gates green (TYPES=0 LINT=0 BUILD=0); **`0029` applied + verified on dev + staging (`ep-round-wave`) + prod (`ep-orange-glade`)** (table + RLS + `business_settings_all` policy). **Shipped to prod** (PR #99→develop, #101→main); prod healthy, Settings route live. **Owner's only remaining step: fill `/account/settings`** (data entry — feature is live). Prod/staging migrations applied by Claude using the in-container `NEON_POSTGRES_DB` (=prod direct conn) + `NEON_API_KEY` (fetched staging conn) — see [[reference-neon-creds-in-container]]. (DONE.)

- **DEV-147 (password reset / forgot-password — 2026-06-15):** Added self-service password recovery (none existed). New public pages **`/auth/forgot-password`** (email → `auth.requestPasswordReset({ email, redirectTo })`; generic "check your email" to avoid account enumeration; redirectTo derived from request `host`/`x-forwarded-proto` so it's correct per-env) + **`/auth/reset-password?token=…`** (new-password form, reuses `validatePassword` + confirm field → `auth.resetPassword({ newPassword, token })` → `/auth/sign-in?reason=reset`). "Forgot password?" link on sign-in; `sign-in-notice` gained the `reason=reset` success message. **Neon Auth's managed service sends the reset email — NO Resend dependency** (`createNeonAuth` takes no email config). Pages are public (proxy guards only `/account`+`/client`). Gates green (TYPES=0 LINT=0 BUILD=0); local smoke test passed (pages 200, link present, no-token→invalid msg, with-token→form). **⚠️ Unverified by Claude: actual email DELIVERY** (needs an inbox) — if Neon doesn't deliver, it's a Neon Console email setting, not a code change; same config for staging + prod. (PR pending → develop/staging → main.)

- **DEV-146 (transparent light/dark wordmark — supersedes DEV-145, 2026-06-03):** Replaced the navy-chip logo with a clean **theme-aware transparent wordmark swap**. New art came from **ChatGPT** (not Grok) — which exported a **real transparent PNG** with correct "EndlessWorlds" spelling + the glowing network ∞ behind "Worlds". Light art is genuinely transparent (navy "Endless"); **derived the dark variant from the same master by recoloring the navy text → off-white** (`#F8FAFC`, ImageMagick `-fuzz 17% -opaque`), so both are pixel-identical except text color. Assets `apps/web/public/logo-wordmark-{light,dark}.png` (490×160, true alpha). Header/footer now render both via `next/image` with a Tailwind `dark:hidden` / `hidden dark:block` swap (no chip — `@custom-variant dark (&:is(.dark *))` + next-themes `attribute="class"` drive it). Removed the old chip `logo-wordmark.png`. Favicon (DEV-144) unchanged. Gates green (TYPES=0 LINT=0 BUILD=0); verified render (both assets + swap classes in header h-10 + footer h-9, serve 200, old chip → 404). **Lesson:** ChatGPT image-gen exports true transparent PNGs; Grok is JPEG-only (see [[reference-grok-logo-workflow]]). (PR pending → develop → main.)

- **DEV-145 (integrated wordmark logo — 2026-06-03):** Replaced the ∞-mark + text lockup in header/footer with an integrated **"EndlessWorlds" wordmark** logo (Grok: dual-color text, globe-as-"o", glowing ∞ network behind "Worlds"). **Grok only exports JPEGs** (baked checkerboard, no real alpha) so the glowing edges can't be cleanly keyed → solution = mount the keyed **dark** logo on a subtle rounded **navy chip** (`#0F1A30`), used identically in light + dark mode (chip hides the edge fringe AND backs the light-colored "Endless" text on white headers). Asset `apps/web/public/logo-wordmark.png` (475×200, transparent outside the chip, built via ImageMagick key→clean→chip). Wired into `site-header.tsx` (h-10) + `site-footer.tsx` (h-9) via `next/image`; **removed orphaned `logo-mark.png`**; ∞ tile **favicon (DEV-144) unchanged**. Gates green (TYPES=0 LINT=0 BUILD=0); verified render (wordmark `<img>` in header+footer, serves 200, old logo-mark.png → 404). Source artwork in `.temp/` (gitignored). **Future:** a true transparent PNG/SVG from another tool would let us drop the chip and do a clean light/dark swap. (PR pending → develop → main.)

- **DEV-144 (brand favicon + logo mark — 2026-06-02):** Shipped the EndlessWorlds infinity (∞) brand mark (Grok-generated, azure→violet gradient on deep navy). **Favicon/app-icon set** from the navy app-icon tile via ImageMagick → `apps/web/src/app/favicon.ico` (multi-size 16/32/48), `icon.png` (512), `apple-icon.png` (180) — Next App Router file conventions auto-emit the `<link rel="icon">`/`apple-touch-icon` tags. **Logo lockup** (transparent mark + `EndlessWorlds` wordmark) added to `site-header.tsx` (h-7, `priority`) and `site-footer.tsx` (h-6) via `next/image`, source `apps/web/public/logo-mark.png` (keyed from the transparent Grok export, 320×160, alpha). Gates green (TYPES=0 LINT=0 BUILD=0); **verified rendered** (icon link tags + 1x/2x optimized logo `<img>` in head/header/footer; favicon.ico/icon.png/apple-icon.png/logo-mark.png all serve 200). Source artwork kept in `.temp/` (gitignored). (PR pending → develop → main.)

- **Release + git cleanup (2026-06-02):** Brought **prod current** — shipped the 2 outstanding `develop` commits (DEV-79 audit-tracking memory-bank sync, `[skip ci]`, docs-only/behavior-neutral) to **`main`** via PR; `develop` and `main` back in sync. **Claude Code permission-audit tooling** added to the devcontainer (`.devcontainer/postinstall.sh` — renamed from `post-install.sh`; rewrites the global `~/.claude/settings.json` allow/deny block; `scripts/claude-audit.sh` rewritten to match permission-denial events → triggering commands + a `--fix`/`--fix-all` mode; new `make` targets `claude-fix*`, `postinstall`, `claude-settings-reset`, `claude-clear-approvals`) — the devcontainer script is now uniformly named `postinstall.sh` and every reference (devcontainer.json `postCreateCommand`, Makefile targets, claude-audit.sh `--fix`, README/CONTRIBUTING/techContext) points to it. **Git hygiene:** deleted 6 merged local feature branches + ~33 merged remote branches (all verified merged into `origin/develop`); only `develop`/`main` (+ the checkpoint branch until folded) remain.

- **DEV-79 (security audit — M6, 2026-06-01):** Read-only audit of `apps/web` (auth, RBAC, SQL, data exposure, headers, secrets). **Fundamentals clean** — Drizzle parameterized queries (no injection), secrets only via `process.env`, strong password policy, public invoice/PDF routes leak nothing, generic errors. **Gaps found (all ticketed, no code changed this pass — tool layer was unstable):** (1) **mutating admin server actions lack a role check** — only `auth.getSession()` + `user.id` scoping, so a `team_member` could POST directly (bounded: writes scope to their own `user.id` ⇒ at worst an inert orphan row in their own namespace) → **DEV-142** (highest severity; exhaustive ~31-action list attached there); (2) **no security headers** (HSTS/CSP/X-Frame/etc.) + **no upload content-type allowlist** + **no app-layer auth rate limiting** → **DEV-143**. Full findings on the DEV-79 Linear comment. **DEV-79 stays In Progress until DEV-142 + DEV-143 land.**

- **DEV-141 (RBAC team-member role — M4, phase 3 of 3 — COMPLETES RBAC, 2026-05-31):** `role=team_member` may sign into `/account` but is **timesheet-only**. **Per-tenant time scoping via `time_entries.logged_by`** (migration `0028`; nullable, "null = logged by owner"). A team member's entry is stored with `user_id` = the **tenant** (the consultant — resolved by `getTenantOwnerId` in rbac.ts as the **oldest admin account**) so it flows into the tenant's timesheet + **invoicing with ZERO change to the owner-scoped billing queries**; `logged_by` records who actually entered it. **Section gating:** every admin `/account/*` page calls `await requireAdmin()` (team → `/forbidden`), the dashboard redirects team → `/account/timesheets`, the **4 admin route handlers** (reports tax / timesheets-csv / timesheets-pdf, documents download) return **403** for non-admins, and the nav shows team members only Timesheets. Timesheets page/action are tenant-aware (team logs against the tenant's companies; admin sees own+team with a "team" tag; team sees only their own; team deletes only their own). Gates green (`TYPES=0 LINT=0 BUILD=0`); **faithful round-trip PASSED (19 assertions)** (team logs 3h → user_id=tenant/logged_by=team/billable+unbilled+companyA; matches buildInvoiceDraft filter; team sees own 3h; team → invoices/reports/payments/companies/access ⇒ /forbidden; team → /account ⇒ /account/timesheets; team → CSV+PDF routes ⇒ 403; admin invoices/reports/CSV unaffected = 200). **Known limitation (defense-in-depth follow-up):** mutating admin server actions still scope by `session.user.id`, so a hand-crafted POST by a team member could create an inert orphan row in their OWN namespace (invisible to admin, unreachable via the gated UI); read/nav surface is fully gated (the action-guard gap is tracked as DEV-142). **Shipped to prod** — PR #84 (develop) + PR #85 (develop→main); `0028` applied to dev + staging + **prod** (verified). **→ DEV-69 RBAC COMPLETE (foundation + client portal + team role), all in production.**

- **DEV-140 (RBAC client portal — M4, phase 2 of 3, 2026-05-31):** Read-only **`/client/*`** portal for `role=client` users (separate from the admin `/account/*`). New `client/layout.tsx` (`getAccess` → clients only; admin/team redirect to their own home, not 403) + `client-nav`, **Dashboard** (company name, outstanding balance, open-invoice + project counts), **Invoices** (issued only — drafts hidden; View + PDF via the existing public token routes), **Projects** — all scoped to the client's `company_id` via shared `lib/client-data.ts` (`getClientContext`/`getClientInvoices`/`getClientProjects`; outstanding = amount − issued credit notes − payments). Proxy matcher extended to `/client/:path*`. **Role-aware post-auth redirect:** sign-in/sign-up now land each role on its home via `roleForEmail` + `homePathForRole` (client → `/client`, admin/team → `/account`). No migration (roles already on `allowed_emails`). Gates green; **faithful round-trip PASSED** (client sees only their company's issued invoice + project; draft hidden; other company's invoice/project hidden; $1,000 outstanding on dashboard; client → /account ⇒ /forbidden + blocked from /account/access; admin → /client ⇒ /account; client sign-in form redirects to /client). **→ Remaining RBAC: DEV-141 (team-member role).** (PR pending.)

- **DEV-69 (RBAC foundation — M4, 2026-05-31):** Role-based access control, phase 1 of 3. **Roles live on `allowed_emails`** (`role` default `admin` + `company_id`; migration `0027`, ADD COLUMN only — backfills every existing row to `admin`, no new table so the table's RLS still applies). The invite IS the role assignment; role resolves from the signed-in user's email at session time — no separate role table. New **`lib/auth/rbac.ts`**: `getAccess()` (session + role + companyId, or null when unauthenticated/revoked), `requireRole(roles)` / `requireAdmin()` / `requireUser()` page guards (no session/revoked → `/auth/sign-in?reason=auth`; wrong role → `/forbidden`), `homePathForRole`. The **`/account` layout now `requireRole(["admin","team_member"])`** (clients are bounced to `/forbidden`; their `/client` portal is DEV-140). New **`/forbidden`** 403 page + **session-expired notice** on the sign-in page (`?reason=auth|expired`, proxy `loginUrl` now carries `reason=auth`). New admin **Access management** page `/account/access` (admin-only, nav-gated): invite an email with a role (+ company for clients) and revoke (soft-delete); `inviteEmail`/`revokeEmail` actions are admin-gated server-side, can't self-revoke, upsert-revive by email. **Roles:** admin (full), team_member (timesheet-only — gating is DEV-141), client (read-only company portal — DEV-140). Gates green; **faithful round-trip PASSED** (11 assertions: admin loads /account + /account/access; invite persists role+company; client signs in → /account redirects to /forbidden + blocked from /account/access; /forbidden renders 403; sign-in notice; self-revoke blocked; revoke soft-deletes; revoked user → sign-in). `0027` on dev. **Audit logging of access changes deferred to DEV-136.** Sub-issues **DEV-140** (client portal) + **DEV-141** (team-member role + per-tenant time scoping) created for phases 2–3. (PR pending.)

- **DEV-74 (timesheet reporting & export — M12 FINAL, 2026-05-31):** New **timesheet report** at `/account/reports/timesheets` (linked from the reports hub). A no-JS **GET filter form** (date from/to + client + project) drives the view; results show **summary cards** (total / billable hrs / utilization % / billable value per currency), **by-project** (hours, billable hrs, amount), **by-month**, and a **client-ready detail** table. Pure `buildTimesheetReport` (lib/reports.ts) does the rollups (amount = Σ billable hrs × snapshotted rate, per-currency, no FX). **Exports** carry the same filters: **CSV** route `/account/reports/timesheets/export` (per-entry detail) + branded **PDF** route `/account/reports/timesheets/pdf` (new `lib/pdf/timesheet-pdf.tsx`, EndlessWorlds header/footer like the invoice PDF). Shared `lib/timesheet-report-query.ts` parses/validates filters + fetches owner-scoped rows so page/CSV/PDF can't disagree. No schema change (reads existing `time_entries` + companies/projects joins). Gates green; **unit test PASSED** (16 assertions) + **faithful round-trip PASSED** (page 20h/18 billable/90%/$1,800+€320; CSV 5 rows w/ 600/1200/320; PDF `%PDF-` 3.7KB; `from=` filter → 12h/100%; client filter → Globex-only; unauthed export → 307). **Deferred:** REST `/api/timesheets` CRUD endpoints from the ticket — the app uses **server actions** (CRUD already lives at `/account/timesheets`); a public REST API isn't needed for a solo internal tool. Code-only — ships next develop→main. **→ M12 (Financial Reporting) COMPLETE** (DEV-132/133/134/135/74). (PR pending.)

- **DEV-134 (time-utilization analysis — M12, 2026-05-31):** **Time utilization** section on /account/reports — billable vs non-billable hours, **overall** + **by month**. Pure `buildUtilizationReport` (lib/reports.ts) sums `time_entries.hours` split on `billable` (skips hours≤0), per month (`work_date` YYYY-MM), returns billable/nonBillable/total + `pct` (billable ÷ total, 1-dp). UI: 4 summary cards (Utilization % / Billable / Non-billable / Total hrs) + by-month table. No schema change. Gates green; **unit test PASSED** (7 assertions: total=16, billable=14, pct=87.5, May=75%, Jun=100%, month order) + **render round-trip PASSED** (seeded 6h+2h May, 8h Jun → page shows 87.5% / Billable 14 / May 75% / Jun 100%). **Profit margin DEFERRED** — no labor-cost/rate model is tracked, so margin can't be computed yet (noted in the UI: "Profit margin needs cost rates — not tracked yet"). Code-only — ships next develop→main. (PR pending.)

- **DEV-135 (tax summary — M12, 2026-05-31):** Tax-collected report on /account/reports. Pure `buildTaxReport` (lib/reports.ts) sums tax on issued invoices (taxable = subtotal − discount; tax = tax_amount; tax>0 only) **by rate/label** + **by month**, per currency. **CSV export** route `/account/reports/tax` (authed) streams per-invoice tax detail (Date/Invoice/Client/Currency/Taxable/Tax label/Tax rate/Tax) for the accountant; "Download CSV" link on the page. No schema change. Gates green; **unit test PASSED** (8 assertions) + **render + CSV round-trip PASSED** (NY Sales Tax 8.875% → $266.25 total; CSV text/csv with 2 taxed rows, no-tax invoice excluded). Code-only — ships next develop→main. (PR pending.)

- **DEV-133 (revenue by client & period — M12, 2026-05-31):** Added a **Revenue (invoiced)** section to /account/reports. Pure `buildRevenueReport` in lib/reports.ts sums issued invoice amounts (excludes draft + credit notes) **by client** and **by month**, per currency. No schema change. Gates green; **unit test PASSED** (9 assertions) + **render round-trip PASSED** (Rev Co $1,500; Apr $1,000 / May $500; draft excluded). **By-project** revenue deferred (invoices aren't project-scoped — only line items reference time entries); interactive **date-range filter, charts, CSV export** deferred (CSV pairs with DEV-137). Code-only — ships next develop→main. (PR pending.)

- **DEV-132 (A/R aging & outstanding — M12 start, 2026-05-31):** Read-only Accounts-receivable report at **/account/reports** (nav: Reports). Pure `src/lib/reports.ts` `buildAgingReport(rows, today)` buckets each unpaid invoice's outstanding (= amount − issued credit notes − payments) into **current / 1–30 / 31–60 / 61–90 / 90+** by days past due, grouped **per currency** (no FX) then per client, with summary cards (outstanding / overdue / current). Excludes draft + paid. No schema change (derived from invoices+payments+credit notes). Gates green; **unit test PASSED** (14 assertions on bucketing/aggregation via Node type-strip) + **render round-trip PASSED** (seeded invoices+payment → page shows $1,300 outstanding / $800 current / $500 overdue). Code-only — ships on next develop→main. Filters by client/date noted as a later enhancement. (PR pending.)

- **DEV-125 + DEV-127 (payments & partial payments — M10 start, 2026-05-31):** Manual payment ledger (checks/bank transfer/cash/card/other) — no Stripe. New `payments` table (migration `0026`, RLS + `payments_all` policy, soft-delete, owner-scoped): invoice_id, amount, method, reference (e.g. check #), received_date, notes. Added `partial` to INVOICE_STATUSES + `PAYMENT_METHODS`. New **/account/payments** page + `AddPaymentForm` (invoice select prefilled with outstanding, amount/method/reference/date/notes) + actions (`recordPayment`, `deletePayment`). **Reconcile:** `recomputeInvoiceStatus` sets paid/partial from Σ(payments)+Σ(issued credit notes) vs amount; removing payments reverts paid/partial→sent. Invoices list shows **paid · credited · outstanding** + a `partial` badge; nav gains Payments. Outstanding = amount − credit notes − payments (DEV-127 multi-payment). Gates green; **faithful round-trip PASSED** ($400→partial/$600 out; +$600→paid; delete reverts paid→partial→sent; $200 credit + $300 payment→paid; zero rejected; RLS on). `0026` on dev + staging. **Stripe/online pay-links/auto-reconcile (DEV-77/126/128) deferred** — checks reconcile manually. (PR pending.)

- **DEV-76 PART 2 (invoice email delivery — M9, 2026-05-31):** "Send" emails the branded PDF to the client via **Resend**. Invoices gain `sent_at` + `sent_to` (migration `0025`). New `src/lib/email.ts` (`sendEmail` via Resend REST, no SDK; `from` = `EMAIL_FROM` env, default Resend shared `onboarding@resend.dev`). New `sendInvoice` action: resolves recipient (override → company primary contact → legacy contact email), renders the PDF (shared `invoicePdfDataFrom` mapper — refactored the PDF route to reuse it too), sends with PDF attachment + public-link HTML, then marks the invoice `sent` (sent_at/sent_to). Per-row **Send** button (`SendInvoiceButton`) + sent-date on the invoices list. Sending domain **`mail.endlessworlds.xyz` is verified in Resend** (DKIM/SPF), so the code default `from` is `EndlessWorlds <invoices@mail.endlessworlds.xyz>` (override via `EMAIL_FROM`). Gates green; **faithful round-trip PASSED with REAL sends to a non-owner client address** (robert@usrelocators.com) via both a direct probe (got a message id) and the Send action (`sent_at`/status=sent); no-recipient guard refuses to mark sent. **→ DEV-76 fully done (PDF + email), email works for real clients.** **DEPLOY NOTE:** set `RESEND_API_KEY` in Vercel prod (it's only in local `.devcontainer/.env`); `EMAIL_FROM` optional (default uses the verified domain). `0025` on dev + staging. (PR pending.)

- **DEV-104 (company document storage — M9, 2026-05-30):** Files attached to a company. New `company_documents` table (metadata: name, content_type, size, storage_key, storage_url) + `document_blobs` dev/test fallback (migration `0024`, both RLS + soft-delete). New `src/lib/storage.ts` with **two drivers**: **Vercel Blob** when `BLOB_READ_WRITE_TOKEN` is set (prod), else a **DB fallback** (bytes base64 in document_blobs) so it works + is testable without external network. New `@vercel/blob` dep. Managed on the company edit page (**Documents card**: upload ≤10MB / list / download / soft-delete) via `documents-actions.ts`; authed owner-scoped **`/account/documents/[id]/download`** route streams the bytes. Gates green; **faithful round-trip PASSED** (form upload → row + stored bytes; authed download returns exact file; unauthed → 307 sign-in; delete soft-deletes + purges bytes; RLS on both). `0024` on dev + staging. **DEPLOY NOTE: set `BLOB_READ_WRITE_TOKEN` in Vercel prod** to use real Blob (until then prod uses the DB fallback). Unblocks real receipt upload for DEV-123. (PR pending.)

- **DEV-76 PART 1 (invoice PDF generation — M9, 2026-05-30):** Branded invoice **PDF** via `@react-pdf/renderer` (pure JS, renders in a Node route handler — no headless browser). New `src/lib/pdf/invoice-pdf.tsx` (EndlessWorlds LLC header, billed-to, line-items table, subtotal/discount/tax/total, payment/Net-terms section, footer) + public **`/invoice/[token]/pdf`** route handler (`runtime=nodejs`, reuses the DEV-122 token, not proxy-guarded) streaming `application/pdf`. **Download PDF** links on the public invoice view + invoices list. Code-only (no migration); new dependency `@react-pdf/renderer@4.3.0`. Gates green (incl. build bundling react-pdf); **faithful round-trip PASSED** (public no-cookie GET → 200, `%PDF-`, 3.2 KB, inline filename; bad/non-uuid token → 404). **Email delivery DEFERRED** (needs an email provider + secret — Resend/SES); the "Send" action + send-status tracking land when that's configured. (PR pending; ships to prod on next develop→main.)

- **DEV-124 (mileage & reimbursables — M9, 2026-05-30):** Mileage implemented as a specialized expense (reuses the `expenses` table — DRY): added `Mileage` category + `distance` + `unit_rate` columns (migration `0023`) + `DEFAULT_MILEAGE_RATE` (0.70). The expense form has an **Expense/Mileage type toggle**; in mileage mode it captures distance + per-mile rate (prefilled, **configurable** per entry) and `addExpense` computes amount = distance×rate server-side. Mileage flows into invoices via the existing expense path with a `Mileage — N mi @ $rate/mi` line (qty=distance, unit=rate). Expenses list shows the mileage detail. Generic reimbursables already covered by DEV-123. Gates green; **faithful round-trip PASSED** (form mileage 100mi×$0.70=$70, distance/rate stored; zero-distance rejected; invoice line + billed-stamp correct). `0023` on dev + staging. **→ M9 expenses/mileage done; remaining: DEV-76 PDF/email, DEV-104/105 docs.** (PR pending.)

- **DEV-123 (expense logging + receipts — M9, 2026-05-30):** New `expenses` table (migration `0022`, RLS + `expenses_all` policy, soft-delete, owner-scoped): company_id, project_id (opt), expense_date, category (Travel/Meals/…), amount, billable, notes, receipt_key, billed_at/billed_invoice_id. New **/account/expenses** page + `AddExpenseForm` (company/project/date/category/amount/billable/notes) + actions (`addExpense`, `deleteExpense` — billed expenses can't be deleted). `buildInvoiceDraft` now pulls **billable, unbilled expenses** for the company into line items (`sourceType='expense'`) across **all** billing types (hourly/retainer/fixed/milestone) and adds them to subtotal; generate + manual create stamp them billed (reusing the partial-billing source path). generateInvoice guard relaxed so an expense-only invoice can be generated. Nav gains Expenses. **Receipt file upload deferred to DEV-104** (document storage) — `receipt_key` column + an optional text reference are in place now. Gates green; **faithful round-trip PASSED** (form-logged billable $100 + non-billable $50; generate → $275 incl. $150 time + $100 expense, billable stamped, non-billable untouched, no double-bill; RLS on). `0022` on dev + staging. (PR pending.)

- **DEV-122 (invoice viewed status + read receipts — M9, 2026-05-30):** Added `viewed` to INVOICE_STATUSES (draft→sent→viewed→paid→overdue). Invoices gain `public_token` (uuid, unique, `defaultRandom` — migration `0021` backfills existing rows + new ones automatically) + `viewed_at`. New **public no-login route `/invoice/[token]`** (outside the proxy's `/account/*` matcher) renders a read-only branded invoice and **records the open**: stamps `viewed_at` on first view + promotes a `sent` invoice to `viewed` (idempotent — timestamp preserved on re-views). Invoices list shows a **Public link ↗**, a `viewed` status badge, and the viewed date. Tokenized link chosen over the email open-pixel (which needs DEV-76). Gates green; **faithful round-trip PASSED** (no-cookie GET→200, sent→viewed + viewed_at set; 2nd GET idempotent; bad/non-uuid token→404; 0 null/dup tokens). `0021` on dev + staging. (PR pending.)

- **DEV-121 (credit notes & refunds — M9, 2026-05-30):** Credit notes as a third document `type` on the invoices table (`credit_note`; migration `0020` adds `credited_invoice_id`). A credit note links to the invoice it credits, carries line items + a reason, and computes subtotal→tax→total from the credited invoice's company tax. New **/account/credit-notes** page + `AddCreditNoteForm` (invoice select, line editor, live totals) + actions (`createCreditNote`, `updateCreditNoteStatus` issued|void, `deleteCreditNote`). Numbering `PREFIX-CN-NNNN`. **Effective outstanding** on the invoices list = invoice.amount − Σ(issued credit notes); voiding a credit note restores the balance. Nav gains Credit notes. **Refund records deferred to M10** (need the payments model). Gates green; **faithful round-trip PASSED** ($1100 invoice → $220 credit note → $880 outstanding → void → $1100 → re-issue → $880; types separated). `0020` on dev + staging. (PR pending.)

- **DEV-120 (quotes/estimates → invoices — M9 start, 2026-05-30):** Quotes implemented as a **`type` discriminator** on the invoices table (`type` = invoice|quote, default invoice; migration `0019`, also `valid_until` + `source_quote_id`) — reuses the entire money/line-item engine (computeInvoiceTotals, invoice_line_items, the editor) with no duplication. Quote statuses: draft/sent/accepted/declined/expired (`QUOTE_STATUSES`). New **/account/quotes** page + `AddQuoteForm` (company select, line editor, discount, valid-until; live totals) + quotes-actions (`createQuote`, `updateQuoteStatus`, `deleteQuote`, `convertQuoteToInvoice`). Quotes number on a separate `PREFIX-Q-NNNN` sequence. **Convert to invoice** copies the quote's agreed money snapshot + line items into a new draft invoice (`source_quote_id` links them, normal invoice number), marks the quote accepted, and is idempotent (no duplicate; UI shows "Invoiced ↗"). Shared `parseLineItems`/`parseDiscount` extracted to `lib/invoice-input.ts` (DRY; importable by both action files). All invoice queries (list, dashboard, edit-page, draft numbering) now filter `type='invoice'`. Nav gains a Quotes link. Gates green; **faithful round-trip PASSED** (quote ACME-Q-0001 $1100 w/ tax → sent → convert → invoice ACME-0001 $1100 linked, lines copied, quote accepted; re-convert prevented). `0019` on dev + staging (0 null types). (PR pending.)

- **DEV-139 (edit existing invoice lines + reorder — M8, 2026-05-30):** New invoice **edit page** `/account/invoices/[id]/edit` (EditInvoiceForm) — add/edit/remove line items, **reorder** (move up/down → persists `sort_order` by submit order), edit discount/dates/notes; totals recompute live. New `updateInvoice` server action: re-reads tax from the company, recomputes subtotal→discount→tax→total, **replaces** line items (old ones soft-deleted, new inserted in order). **Guard:** only `draft` invoices are editable — the edit page shows a locked notice for sent/paid, and `updateInvoice` rejects non-draft server-side. Does NOT re-stamp time entries/milestones (settled at create). "Edit" link added to the invoices list for draft rows. No schema change (`sort_order` already existed → code-only, no migration). Gates green; **faithful round-trip PASSED** (create $330 → edit reorder[B,A,C]+change+add+10%disc → $396 w/ 3 active lines, 2 soft-deleted; sent-invoice edit blocked, unchanged). **→ M8 fully COMPLETE (DEV-115/116/117/118/119/139).** (PR pending; code-only so no prod migration — ships on next develop→main.)

- **DEV-119 (fixed-fee & milestone billing — M8 FINAL, 2026-05-30):** `billing_type` extended with `fixed` + `milestone` (alongside hourly/retainer). **Fixed-fee:** company `fixed_amount` → buildInvoiceDraft emits one line "Fixed project fee". **Milestone:** new `company_milestones` table (migration `0018`, RLS + `company_milestones_all` policy, soft-delete, owner-scoped): name, amount, status (pending|invoiced), due_date, invoiced_invoice_id. Managed on the company edit page (Milestones card, shown when billing_type=milestone — add/delete pending). buildInvoiceDraft (milestone) lists PENDING milestones as line items (sourceType='milestone'); generate marks them invoiced; manual form supports **partial** milestone billing via the DEV-118 source-stamping path (`billedMilestoneIds`). Company form has the two new types + a fixed-fee field; companies list `billingSummary` handles all four. **Milestones attach at company level** (the billing/Generate flow is company-centric), a deviation from the ticket's "on a project" — noted on the ticket. Gates green; **faithful round-trip PASSED** (fixed→$5000 1 line; milestone add-via-form + Generate→$3000, 2 invoiced/0 pending; partial→M1+M2 invoiced, M3 pending; RLS on). `0018` on dev + staging. **→ M8 (Invoice Engine v2) core COMPLETE (DEV-115/116/117/118/119).** (PR pending; DEV-139 edit/reorder + batched M8 prod release remain.)

- **DEV-117 (multi-currency — M8, 2026-05-30):** Invoice in a client's currency (was USD-only). `companies.currency` + `invoices.currency` (ISO 4217, default USD; migration `0017` — column DEFAULT fills existing rows, no separate backfill). Currency is snapshotted from the company onto each invoice at create (buildInvoiceDraft/generate/createInvoice). New shared `src/lib/money.ts`: `CURRENCIES` allowlist (12 codes), `formatMoney(value, currency)`, `isCurrency`, `DEFAULT_CURRENCY` — used by both server + client. Replaced ALL hardcoded USD `Intl.NumberFormat` formatters (invoice form, invoices list, companies list, projects list, generate button) with currency-aware `formatMoney`. Company form has a currency picker; `parseCompany` validates against the allowlist. **No FX conversion** (v1) — amounts stay in their own currency; cross-currency reporting will aggregate per-currency (M12). Gates green; **faithful round-trip PASSED** (onboard→CAD; Generate→EUR; manual form→GBP; 0 null currencies). `0017` on dev + staging. (PR pending.)

- **DEV-118 (discounts & partial billing — M8, 2026-05-30):** Invoice-level discount + partial billing on the money model. Invoice gains `discount_type` (percent|fixed|null), `discount_value`, `discount_amount` (migration `0016`, columns + backfill discount_amount=0). Money order is now **subtotal → discount → tax (on discounted base) → total**; `amount = (subtotal − discount) + tax`. New `computeDiscount` + `computeInvoiceTotals(subtotal, company, discount)` in invoicing.ts is the single source of truth (discount floored 0, capped at subtotal; percent ≤ 100). `createInvoice` parses discount + line `sourceId`s. **Partial billing:** line items now carry `sourceType`/`sourceId` through the form; `createInvoice` stamps billed ONLY the time entries whose lines were submitted AND are in the draft (drop a time line → it stays unbilled). `generateInvoice` (one-click) still bills everything. Invoice form has a discount control + live Subtotal/Discount/Tax/Total; invoices list shows the breakdown. Per-line discount deferred (invoice-level only). Gates green; **faithful round-trip PASSED** (percent 10%+10% tax → $990; fixed $50 → $450; partial → only submitted entry stamped). `0016` on dev + staging (0 null discounts, 0 amount mismatches). (PR pending.)

- **DEV-116 (invoice tax — M8, 2026-05-30):** Configurable per-invoice tax. Company gains `tax_rate` (percent), `tax_label`, `tax_exempt`; invoice gains `subtotal`, `tax_rate`, `tax_label`, `tax_amount` (migration `0015`, columns only + backfill: existing invoices get subtotal=amount, tax=0). `amount` stays the grand total = subtotal + tax. New `computeTax(subtotal, company)` in invoicing.ts is the single source of truth (exempt or 0 rate → no tax, null rate/label); `buildInvoiceDraft`, `generateInvoice`, and `createInvoice` all snapshot tax onto the invoice (server-authoritative). Company form has tax-rate/label/exempt fields; invoice form shows Subtotal / Tax (label + %) / Total live; invoices list shows the tax breakdown. Per-line tax not done (per-invoice only — noted on ticket). Gates green; **faithful round-trip PASSED** (8.875%→$31.06 on $350; tax-exempt→$0; manual 10% VAT→$100 on $1000). `0015` on dev + staging (0 invoices missing backfill, 0 amount mismatches). Feeds tax-summary report (M12). (PR pending.)

- **DEV-115 (invoice line-items — M8 keystone, 2026-05-30):** New `invoice_line_items` table (migration `0013` = table; `0014` = RLS `invoice_line_items_all` policy + backfill — split because 0013 shipped without them; soft-delete, owner-scoped): description, quantity, unit_amount, line_total, sort_order, source_type/source_id. Invoice `amount` is now the **total = Σ(line_total)**. `buildInvoiceDraft` emits **line items** (one per billable time entry for hourly, one flat line for retainer); `generateInvoice` + `createInvoice` persist header + lines via shared `insertInvoiceLineItems`. The **New invoice form is now a line-item editor** (add/remove rows, live totals, prefilled on company-select; submits a `lineItems` JSON field, total derived server-side). Migration **backfills** every existing invoice as one line. Invoices list shows the lines. Gates green; **faithful dev round-trip PASSED** (generate → $350 w/ 2 per-entry lines; manual form → $1500 w/ 2 custom lines; backfill verified). `0013`+`0014` on dev + staging (RLS verified; backfill: every active invoice has exactly one line). Unblocks tax/discounts/multi-currency/PDF (rest of M8 + M9). (PR pending.)

- **DEV-110 (multiple contacts per client — M7 FINAL, 2026-05-30):** New `company_contacts` table (migration `0012`, RLS + `company_contacts_all` policy, soft-delete, owner-scoped): name, email, phone, role, is_primary. Managed on the company **edit page** (add / delete / make-primary; setting a primary demotes the others). Migration **backfills** each company's legacy single contact (`contact_name`/`contact_email`) as primary; onboarding a new company also seeds a primary contact (saveCompany create-sync). Gates green; **faithful dev round-trip PASSED** (onboard→primary contact; add 2nd; make-primary demotes; delete). `0012` on dev + staging. **→ M7 (Client & Project Depth) COMPLETE (DEV-109/110/111/112/113/114 all Done).** (PR pending; batched prod release next: migrations `0010`+`0011`+`0012`.)

- **DEV-114 (time by project + task — M7, 2026-05-30):** `time_entries.task` (migration `0011`, free-text) — optional task/activity on the timesheet form; shown under the project in the entries list. (Project picker already shipped in DEV-109.) Gates green; **faithful dev round-trip PASSED** (logged "Discovery call" → stored). `0011` on dev + staging (prod batched for M7-end release). **M7 nearly complete — only DEV-110 (multiple contacts) remains.** (PR pending.)

- **DEV-111 (client payment terms — M7, 2026-05-30):** `companies.payment_terms_days` (migration `0010`, default 30; 0 = due on receipt). Onboarding/edit form has a **Payment terms** select (Due on receipt / Net 15/30/45/60); `buildInvoiceDraft` computes the invoice due date from it (was the hardcoded `NET_TERMS_DAYS`). Gates green; **faithful dev round-trip PASSED** (Net 15 company → invoice issue→due = 15 days). `0010` applied to dev + staging (prod migrations `0010`+ batched for the M7-end release). (PR pending.)

- **DEV-112 (billable vs non-billable — M7, 2026-05-30):** `time_entries.billable` boolean (migration `0009`, default true). Timesheet form has a **Billable** checkbox (default on); list marks non-billable entries. `buildInvoiceDraft` now bills **only billable** unbilled hours. Gates green; **faithful dev round-trip PASSED** (billable 2h + non-billable 2h @ $150 → invoice $300, only the billable entry stamped billed). `0009` on dev + staging. (PR pending; feeds utilization reporting DEV-134.)

- **DEV-113 (rate resolution — M7, 2026-05-30):** `time_entries.rate` (migration `0008`) snapshots the effective hourly rate at log time, resolved **entry override → project rate → company rate** (null for retainer/rateless). Timesheet form gained an optional **Rate override**; entries list shows the rate. `buildInvoiceDraft` now sums **Σ(hours × per-entry rate)** (falls back to company rate for legacy entries) and labels the note `@ $X/hr` or `(mixed rates)`. Gates green; **faithful dev round-trip PASSED** (company $150 / project $200 / override $300 → invoice $650, "mixed rates"). `0008` on dev + staging. (PR pending; pairs with DEV-112 billable flag.)
- **DEV-109 (projects/engagements entity — M7 start, 2026-05-30):** New `projects` table (migration `0006` create + FK; `0007` adds the `projects_all` RLS policy + `time_entries.project_id`), soft-delete, owner-scoped: company_id, name, status (active/closed), optional hourly-rate override, start/end, notes. CRUD at `/account/projects` (list + form + `[id]/edit`); **Projects** in portal nav. `time_entries.project_id` is a plain uuid (no hard FK — same as `billed_invoice_id`; dodges a drizzle-kit FK-graph resolution quirk). Timesheet form has an optional project picker filtered by the selected company (validated server-side); entries list shows the project. Gates green; **faithful dev round-trip PASSED** (create $200/hr project → log time → project_id stored + join resolves). `0006`+`0007` applied to **dev + staging**. Foundation for DEV-113 (rate resolution) + DEV-114 (task tracking). (PR pending.)
  - ⚠️ Migration-meta hygiene note: keep schema edits + `drizzle-kit generate` together and verify `_journal.json` gains the new tag before migrating — chaotic interleaving left a 0007 .sql unregistered in the journal mid-session (fixed by regenerating cleanly).
- **Prod release + roadmap (2026-05-30):** Shipped the company/client portal + invoicing core to production (**PR #42**, `c730187`); prod Neon already at migration `0005` (verified). Created the full invoicing-platform roadmap in Linear — **milestones M7–M13** with **DEV-109–138** + reused DEV-69/74/76/77/104/105 — mirrored at **`docs/roadmap.md`**. Keystone is **DEV-115 (invoice line-items refactor)**. DEV-101/102/103/106/107/108 closed **Done**. Merged worktrees cleaned up.
- **DEV-108 (invoice form: company-first + auto-fill):** The "New invoice" form now leads with the **Company** picker; selecting one **auto-fills** number/amount/dates/notes (from the latest completed period), all editable. **Saving marks the underlying unbilled hours billed** for hourly companies (so they can't be double-billed) — the form is now an editable "Generate". Extracted a shared **`src/lib/invoicing.ts` `buildInvoiceDraft(company, userId)`** used by the Generate button, the form prefill, and `createInvoice` so they can't drift. No new migration (uses 0005). Gates green; **faithful round-trip PASSED on dev** (page embeds ACME-0001/$750 prefill; form create → invoice + both entries billed). (PR pending.)
- **DEV-107 (timesheet UX):** Company picker on the timesheet form is now a controlled field, so it **stays selected after "Log time"** (React 19 resets uncontrolled form fields after a server action). Date/hours/notes still clear for the next entry. Gates green. (PR pending.)
- **DEV-103 (invoice prefix + generate-invoice engine):** Migration `0005` — `companies.invoice_prefix`, `time_entries.billed_at`/`billed_invoice_id`. Company onboarding/edit now has an **invoice prefix** (auto-suggested from name, editable). **"Generate invoice"** button (companies list row + edit page) → `generateInvoice` action creates a **draft** invoice for the latest completed billing period (`src/lib/billing.ts` `latestCompletedPeriod`, unit-checked): hourly = sum of unbilled entries up to period end × rate (entries stamped `billed_at`/`billed_invoice_id` so they can't be re-billed); retainer = flat amount. Numbers = `{PREFIX}-{seq}` (count incl. soft-deleted +1). Invoices list shows the period/source note. Gates green; **faithful generate round-trip PASSED on dev** (ACME-0001 = $750 for 5h@$150, both entries billed, 2nd generate adds nothing). `0005` applied to **dev + staging** (Neon API path). Future auto-generation → Neon **pg_cron** calling the same generator. (PR pending.)
- **DEV-106 (portal server actions broken by auth middleware — FIX):** Onboarding (and every portal mutation) failed in the browser with *"An unexpected response was received from the server"* and **no Vercel error log**. Root cause: `src/proxy.ts` (`auth.middleware`) 307-redirected the server-action **POST** to `/auth/sign-in`; React's action dispatcher can't parse the redirect → throws. Fix: middleware guards **GET** only; non-GET passes through (`NextResponse.next()`), actions self-guard via `getSession()`. Verified on dev: faithful no-JS action POST → 200 + company row created; unauthed GET `/account/*` still 307→sign-in; gates green. Latent since DEV-85 (portal actions were only DB-script-verified, never browser-submitted). See `systemPatterns.md`. **Also: migration `0004` was applied to staging (Neon API path) on 2026-05-29 — staging reads work.** (fix; PR pending.)
- **DEV-101 (client/company entity + onboarding):** New `companies` table (migration `0004`, RLS + `companies_all` policy, soft-delete, owner-scoped) with billing config — `billing_type` hourly|retainer, `hourly_rate`/`retainer_amount`, `billing_frequency` weekly|biweekly|semimonthly|monthly, `billing_anchor_day`. Onboarding UI at `/account/companies` (list + form) + `/account/companies/[id]/edit`; **Companies** in portal nav + dashboard count/quick-action. `time_entries` + `invoices` migrated to `company_id` (FK; legacy free-text `client` now nullable + display fallback); both forms pick a company. Time entries gained `start_time`/`end_time` (hours auto-derive from the span). Gates green; live Neon round-trip PASSED on the **dev** branch (migration applied to dev only — staging/prod apply is a separate deploy step, like 0000–0003). Picker shared via `src/lib/companies.ts`. (PR #?, In Progress.)
- **DEV-102 (auth UX):** Accessible `PasswordInput` reveal toggle (sign-in + sign-up), confirm-password field on registration (server validates match), length-first password policy `src/lib/auth/password.ts` (min 12, screens common/weak — stricter than Better Auth's default 8). Gates green. (PR #36, In Progress.)
- **New tickets from portal testing (2026-05-29):** DEV-101 (company entity, ↑), DEV-102 (auth UX, ↑), **DEV-103** invoice accrual from billing period + timesheets (blocked by 101), **DEV-104** company document storage (blocked by 101), **DEV-105** Google Drive sync + PDF export (blocked by 101, ↔ DEV-76).
- **Release + git cleanup (2026-05-29):** Shipped `develop`→`main` via **PR #34** (noindex DEV-99, staging/QA DEV-81, checkpoint skill DEV-100, checkpoint state) — all prod-behavior-neutral; `main` back-merged into `develop` so both are identical at `cf7f8ba`. Cleaned local git: deleted 8 merged feature branches (all verified against merged PRs #20/#22/#24/#25/#27/#28/#29/#30), pruned stale `: gone` remote refs. Only `develop` + `main` remain.
- **DEV-81 (isolated staging/QA — separate Neon project):** Replaced branch-based staging (which shared prod's Neon Auth) with a dedicated Neon project `EndlessWorlds.Staging` (`winter-dew-93819743`): created it, ran migrations 0000–0003 + allowlist seed, enabled Neon Auth via API (`POST /projects/{id}/branches/{br}/auth`, `better_auth`), generated a cookie secret, repointed `develop`-scoped Vercel env at it, added its trusted domain, deleted the orphaned prod-project `staging` branch. `make db-reset-staging` (+ `apps/web/scripts/reset-staging-db.mjs`) wipes `neon_auth` users/sessions **and** app data, hard-guarded to the staging project by name. Verified live. (PR #28; DEV-81 still In Progress — pre-launch walkthrough remains.)
- **DEV-100 (checkpoint skill):** `.agents/skills/checkpoint/SKILL.md` — on-demand memory-bank→docs→Linear→commit+push. (PR #29, Done.)
- **DEV-99 (noindex non-prod):** `isProd = VERCEL_ENV === "production"` in `src/lib/site.ts`; `robots.ts` → `Disallow: /` and `metadata.robots` → `noindex,nofollow` on non-prod. Verified live on staging; prod indexable. (PR #27, Done.)
- **DEV-82 (custom domain):** `endlessworlds.xyz` LIVE (Hover; moved NS off an inaccessible AWS Route 53 zone → Hover NS; apex A → Vercel; www→apex 308; fixed a backwards apex→www redirect; `NEXT_PUBLIC_SITE_URL` flipped to the custom domain). Neon Auth trusted domains updated. (In Progress — email DNS/SPF, GSC sitemap, monitoring remain.)
- **DEV-67 (Analytics → GA4):** Pivoted off Vercel Web Analytics (cost). GA4 via `@next/third-parties` behind a cookie-consent banner (`src/components/site-analytics.tsx`, `useSyncExternalStore`; GA + cookies only after Accept). `sendGAEvent("contact_lead")` on contact success. **Live:** `NEXT_PUBLIC_GA_ID=G-G830M5YF0W` set on prod + redeployed; ID confirmed in the prod bundle. (Done.)
- **DEV-98 (portal access allowlist + sign-in link):** `allowed_emails` table (RLS + `allowed_emails_all` policy; migration `0003`; seeded `mrosmarin@gmail.com`). `isEmailAllowed()` (`src/lib/auth/allowlist.ts`) enforced in sign-up + sign-in actions. Sign-in links in header + footer. Shipped to prod (PR #25/#26). Fixed a live "Invalid origin" blocker by adding prod origin to Neon Auth trusted domains. Caveat: raw `/api/auth/[...path]` not gated (follow-up). (Done.)
- **DEV-65 (SEO foundation):** Metadata API (title template, `metadataBase`, OG/Twitter, canonicals), JSON-LD `@graph`, `sitemap.ts` + `robots.ts`, branded `opengraph-image`. Site config `src/lib/site.ts` (`NEXT_PUBLIC_SITE_URL`). Shipped to prod (PR #21). (Done.)
- **Prod ship + Neon isolation (DEV-88):** M4+M5 promoted to prod (PR #18). Neon `preview` branch for PR previews. Linear board reconciled (18 superseded backlog tickets → Duplicate). (Done.)
- **DEV-95/96/97 (M4/M5 portal):** portal shell + dashboard; timesheets (`time_entries`); invoicing (`invoices`) — all RLS, soft-delete, scoped per user; dashboard "Hours this week" + "Open invoices" wired to live queries. (Done.)
- **DEV-83→94:** bootstrap; monorepo + Next 16 (DEV-84); Neon + Neon Auth + Drizzle (DEV-85); Tailwind v4 + shadcn + dark (DEV-86); CI gated on `RUN_CI=false` (DEV-87); brand/IA `docs/brand-and-ia.md` (DEV-89); public pages Home/Services/About/Work/Contact (DEV-90–94, lead form → Neon). Insights still a stub.

> Next.js 16 has breaking changes vs older docs — consult `node_modules/next/dist/docs/` before writing Next.js code (`apps/web/AGENTS.md`).

## Next steps (resume here)

> All work in a worktree (`make worktree-new TICKET=… SLUG=…`) → PR to `develop`. Never edit the root checkout. (See memory `feedback_worktree_flow`.)

1. **Portal — company/billing depth (in flight):** land **DEV-101** (company entity, PR open) + **DEV-102** (auth UX, PR #36); then **DEV-103** accrual (hours×rate / retainer over billing period), **DEV-104** company doc storage, **DEV-105** Drive sync + PDF (↔ DEV-76). Apply migration `0004` to staging/prod at deploy time (manual, like 0000–0003).
2. **M3 finish:** **DEV-59** insights/blog section (still a stub) + **DEV-66** seed articles; **DEV-63/64** copy polish; **DEV-65/SEO** Core Web Vitals → **DEV-80**.
3. **Portal depth:** **DEV-71** utilities showcase; **DEV-69** RBAC roles; **DEV-72** profile/account settings.
4. **Invoicing depth (deferred):** **DEV-76** invoice PDF/email; **DEV-77** Stripe.
4. **Launch (M6):** **DEV-82** finish (email DNS SPF/DKIM/DMARC, submit sitemap to Google Search Console, uptime/error monitoring); **DEV-78/79/80** cross-browser/a11y, security audit, perf.
5. **Testing gap:** Vitest/Playwright installed but **no real test suites yet** — CI test step still commented out.
6. When ready: **ship `develop`→`main`** to push noindex + staging tooling to prod (behavior-neutral).

## Open questions / decisions

- **Public sign-up hardening:** allowlist is enforced in the app actions only; the raw Neon Auth `/api/auth/[...path]` endpoint isn't gated — lock it down at the Neon Auth/Better Auth layer if it matters.
- **RBAC role model** (admin vs client/team) — not yet designed (DEV-69).
- **Stripe/payments** — deferred (DEV-77).
- Neon Auth is **Beta + AWS-only** — acceptable for MVP.

## Important preferences

- **Never commit/push without explicit user approval**; show staged files + message first.
- **All committed work goes through a worktree branch → PR → `develop`** — no "small tooling/skill" exceptions; keep the root checkout clean.
- Pre-commit hygiene order: memory bank → docs → Linear → commit (+ push). The `/checkpoint` skill runs this on demand.
- Monorepo preferred even for a single app. Soft deletes + RLS on every table.
